[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
word_counter = "pipenv run python -m src.word_counter.word_counter"
format = "ruff format ."
type_check = "mypy ."
bandit = "bandit ."

[packages]
click = "*"

[dev-packages]
mypy = "*"
ruff = "*"
bandit = "*"

[requires]
python_version = "3.12"
